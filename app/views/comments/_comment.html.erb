<!-- <div class="container">
<%#= gravatar_for current_user, #size: 50 %>
<form class="form-inline" role="form">
            <div class="form-group">
                <input class="form-control" type="text" placeholder="Your comments" width="100px"/>
            </div>
            <div class="form-group">
                <button class="btn btn-default">Add</button>
            </div>
</form> -->

<% if logged_in? %>
  <%= link_to gravatar_for(current_user , size: 50), current_user %>
  <%= form_for(@comment = Comment.new) do |f| %>
    <%= f.text_field :content, placeholder: "Comment", class: "comment-box" %>
    <%= f.submit "Post", class: "btn btn-default comment-post-button" %>

    <div class="field">
        <%= f.hidden_field :user_id, :value => current_user.id %>
      </div>
      <div class="field">
        <%= f.hidden_field :entry_id, :value => @entry.id %>
      </div>
  <% end %>
<% end %>


